<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    tools:context="com.example.fabricio.controleusuarios.views.MainActivity">

    <ListView
        android:id="@+id/lista_usuarios"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <!-- aqui e so aumentar o botao e trocar o linearLayout para RelativeLayout -->
    <Button
        android:id="@+id/floating_button"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="16dp"
        android:layout_marginRight="16dp"
        android:background="@android:drawable/radiobutton_off_background"
        android:elevation="6dp"
        android:paddingBottom="16dp"
        android:text="+"
        android:textColor="#4744e4"
        android:textSize="40sp" />

</RelativeLayout>